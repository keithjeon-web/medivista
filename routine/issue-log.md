# MEDIVISTA Routine Issue Log

## 2026-06-19 11:14:42 +09:00

- GitHub/local/deploy state checked: reviewed the current local source, `.deploy-medivista-github` state, and open GitHub Issue #1 covering WooCommerce and Geo-IP requirements; the issue's former separate-subdomain assumption is superseded by the user's 2026-06-19 unified-site decision.
- Implementation result: removed `BRAND SHOP` CTA routing, integrated WooCommerce Shop/Cart/Checkout/My Account functionality into `wp-theme-starter`, and added Shop-only Korean IP blocking with Administrator/Shop Manager exemptions plus South Korea selling/shipping exclusion.
- Files changed: static page headers/footers; `AGENTS.md`; `README.md`; active Shop/WordPress operating docs; WooCommerce import CSV image URLs; `wp-theme-starter` functions/header/footer/style/readme plus new Shop templates, access control, CSS, and JS; recovery tooling and logs.
- Checks run: `node --check` for all active JS; project PHP structural fallback lint; runtime `BRAND SHOP` and shop-subdomain scan; risky-claim scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -RebuildWordPressZip`; ZIP content inspection.
- Result: unified WordPress ZIP readiness PASS; static commerce guardrail remains intact; live WordPress/WooCommerce behavior still requires installation-level testing with WooCommerce and a real trusted GeoIP country header.
- Preserved drift: existing uncommitted `.deploy-medivista-github/assets/css/styles.css` and `.deploy-medivista-github/wp-theme-starter/assets/css/main.css` changes were not overwritten.
- GitHub blocker: local commit `f731038` was created on `feature/unified-shop-integration`, but the push failed on GitHub port 443 and the elevated external upload was not approved, so no PR was opened.

## 2026-06-19 - Products finished-image ordering

- Result: Products now uses all 114 completed WebP product images in a consistent category and product-name order, with only Body Fillers and Hair Treatment retained as preparation cards.
- Mirrored surfaces: static Products page, WordPress Products template/card renderer, shared CSS, and all root/WordPress product CSV sources.
- Counts: 116 visible cards total, 114 ready product images, 2 preparation cards.
- Checks: JS syntax PASS; structural PHP lint PASS; WordPress ZIP readiness PASS; product image count PASS; commerce and risky-claim scan PASS.

## 2026-06-19 - Brand presentation and Shop access update

- Shop access: Korean-IP blocking is temporarily disabled during site construction; reactivation is explicitly marked in `wp-theme-starter/inc/shop-access-control.php`.
- CELLEXOR: rebuilt static and WordPress detail pages using the existing black/gold/ivory brand direction, removed broken encoded copy, and retained conservative professional beauty wording.
- RUMINES: added the brand submenu entry, static and WordPress Coming Soon pages, Brands-page teaser, sitemap entry, and WordPress ZIP requirement.
- Home partners: strengthened title contrast and standardized all partner logo frames and image dimensions.
- MCP/browser limitation: `cellexor.co.kr` returned DNS resolution failure and the reference site could not be inspected from this environment; local recovery preview passed, but the browser client could not reconnect after the preview process ended.
- Checks: JS PASS, structural PHP lint PASS, risky-claim scan PASS, WordPress ZIP PASS, product images PASS.
- Official reference verification: MCP browser inspection succeeded for the supplied CELLEXOR brand and Re:Tone URLs; the local brand and product heroes were refined to match their monochrome cellular and oversized ivory/gold product direction while excluding risky source claims.

## 2026-06-19 - WordPress consolidated update package

- Updated the WordPress deployment deliverables to reflect all current decisions.
- WXR import now contains 12 pages using `medivista.co.kr`, including Rumines and the four WooCommerce pages.
- Theme frontend titles now force the MEDIVISTA site identity; Shop Korean-IP blocking remains temporarily disabled with a production re-enable marker.
- Product import CSV image URLs now use `https://medivista.co.kr/wp-content/themes/wp-theme-starter/`.
- Checks: JS PASS, structural PHP lint PASS, 12-page XML parse PASS, unified WordPress ZIP PASS, product images PASS, commerce/risky-claim scan PASS.

## 2026-06-12 02:39:32 +09:00

- GitHub/local/deploy state checked: read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; confirmed repo `keithjeon-web/medivista` default branch `medivista`; `.deploy-medivista-github` is clean against its index and remains `ahead 12` over `origin/gh-pages`; root static hashes matched deploy hashes before this run's focused edit.
- New issues or PRs found: no new open GitHub work items beyond Issue `#11` and PR `#10`; PR `#10` remains open, mergeable, and already covers the broader MEDIVISTA PT2 site build, so no duplicate branch/PR was created.
- Homepage compatibility result: file-level homepage parity remains intact for shared cache-busted CSS/JS/logo references, Brand Shop routing, English-first navigation, category coverage in nav/products, and catalog-only constraints; mobile nav behavior remains structurally wired through shared `nav-toggle` / `#primary-nav` markup, but visual browser verification was still blocked after closeout because the temporary local preview job does not persist.
- Files changed: `contact/index.html`, `.deploy-medivista-github/contact/index.html`, `wp-theme-starter/page-contact.php`, `docs/dev-log.md`, `docs/error-report-latest.md`, `routine/issue-log.md`.
- Checks run: GitHub repo/issue/PR query; deploy branch/remote/log check; root vs deploy and root vs WordPress starter SHA-256 parity check for shared static files; Brand Shop URL scan; shared header/nav markup scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: public preview parity fetch still failed in this environment; `gh-pages` push was not executed and deploy remains ahead of remote; direct script execution is blocked by PowerShell policy without the `-ExecutionPolicy Bypass` wrapper; post-script local HTTP checks failed because the spawned preview job is transient to that PowerShell process.
- Next recommended action: push `.deploy-medivista-github` from a network-enabled environment, then visually verify the public Home header/logo/mobile nav and the Contact page's expanded product selector.

## 2026-06-12 05:40:20 +09:00

- GitHub/local/deploy state checked: re-read the required docs and cross-checked the current workspace against `.deploy-medivista-github`; the root folder is still not a Git checkout, `gh` is not available in this shell, and direct remote issue/PR/commit refresh was therefore not accessible in this run.
- New issues or PRs found: no new GitHub work items were verified live in this run; the last known active items remain Issue `#11` and PR `#10`, and no duplicate branch or PR was created.
- Homepage compatibility result: shared runtime parity remains intact across root and deploy for `index.html`, all key static subpages, `assets/css/styles.css`, `assets/js/main.js`, and `medivista_logo_header.png`; cache-busted references remain `v=20260611d`, Brand Shop links still target `https://shop.medivista.co.kr`, English-first copy remains in place, all required product categories are present, and the catalog-only/risky-claim scans passed.
- Files changed: `.deploy-medivista-github/contact/index.html`; `docs/dev-log.md`; `routine/issue-log.md`; `docs/error-report-latest.md`.
- Checks run: deploy `status` / `remote -v` / recent `log`; deploy diff review for `contact/index.html`; root-vs-deploy SHA-256 parity checks; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; Brand Shop URL scan; prohibited commerce and risky-claim scans; shared nav/contact wiring scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: public preview fetch still failed in this environment; `gh-pages` push was not attempted here and deploy remains `ahead 13`; the preview server started by the recovery script is still transient to that PowerShell process, so follow-up HTTP checks from this shell failed immediately after closeout.
- Next recommended action: from a network-enabled environment, push `.deploy-medivista-github` `gh-pages` at commit `9a9187b`, then visually verify the public Home header/logo/mobile nav and the Contact page category selector.

## 2026-06-12 08:40:15 +09:00

- GitHub/local/deploy state checked: re-read AGENTS.md, docs/dev-log.md, docs/github-client-preview.md, docs/medivista-web-automation-cycle.md, docs/medivista-automation-prompts.md, docs/error-resolution-automation.md, and docs/error-report-latest.md; confirmed the root workspace is still the local source of truth, .deploy-medivista-github was head 13 before this run's deploy commit, and direct remote refresh remained blocked.
- New issues or PRs found: no new GitHub issue or PR state could be verified live because git ls-remote to GitHub failed on port 443 and gh is not available in this shell; last known active items remain Issue #11 and PR #10, and no duplicate branch or PR was created.
- Homepage compatibility result: local and deploy parity remained intact for the homepage, key static subpages, shared CSS/JS references, medivista_logo_header.png, Brand Shop routing, English-first copy, and category coverage; shared 
av-toggle and #primary-nav wiring for mobile navigation is still present, but browser-level visual verification remained limited by the transient preview job and blocked public fetch.
- Files changed: products/index.html, .deploy-medivista-github/products/index.html, wp-theme-starter/page-products.php, wp-theme-starter/template-parts/product-card.php, wp-theme-starter/assets/data/medivista-products.csv, wp-theme-starter/assets/data/product-category-classification.csv, wp-theme-starter/assets/data/product-image-manifest.csv, docs/dev-log.md, outine/issue-log.md, docs/error-report-latest.md.
- Checks run: deploy status / log / diff; root-vs-deploy parity check; 
ode --check assets/js/main.js; 
ode --check wp-theme-starter/assets/js/main.js; Brand Shop URL scan; commerce/risky-claim scans; category completeness scan; powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip.
- Blockers or network/auth limits: git ls-remote to https://github.com/keithjeon-web/medivista.git failed with Could not connect to server on port 443; public preview parity fetch remains blocked in this environment; the recovery script preview server still does not persist after that PowerShell process exits.
- Next recommended action: push .deploy-medivista-github commit 6adef2e from a network-enabled environment, then visually verify the public homepage header/logo/mobile navigation and the Products page Dermal Fillers label update.

## 2026-06-12 11:41:23 +09:00

- GitHub/local/deploy state checked: re-read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; confirmed the root workspace is still the local source of truth, `.deploy-medivista-github` was `gh-pages...origin/gh-pages [ahead 14]` before this run's deploy commit, and `git ls-remote` to GitHub still fails on port 443 from this environment.
- New issues or PRs found: no newer GitHub issue, PR, or remote commit information was accessible live in this run; the last known active items remain Issue `#11` and PR `#10`, and no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static subpages still share the same `v=20260611d` CSS/JS/logo references, `aria-controls="primary-nav"` and `id="primary-nav"` wiring remains present for mobile navigation, Brand Shop links still target `https://shop.medivista.co.kr`, English-first copy remains intact, required product categories are complete, and main-site catalog-only constraints remain intact; visual browser verification is still limited by the transient preview job and blocked public fetch.
- Files changed: `assets/data/medivista-products.csv`, `assets/data/product-image-manifest.csv`, `assets/data/product-category-classification.csv`, `wp-theme-starter/assets/data/medivista-products.csv`, `wp-theme-starter/assets/data/product-image-manifest.csv`, `wp-theme-starter/assets/data/product-category-classification.csv`, `docs/dev-log.md`, `docs/error-report-latest.md`, `routine/issue-log.md`, and the mirrored deploy checkout data files committed as `.deploy-medivista-github` commit `c661911`.
- Checks run: `git -c safe.directory=... status`, `remote -v`, `log`, and `ls-remote` attempt for `.deploy-medivista-github`; old-taxonomy scan for `HA Dermal Fillers` / `ha-dermal-fillers`; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; `node --check wp-theme-shop/assets/js/shop.js`; Brand Shop URL scan; runtime commerce/risky-claim scan with expected false positives limited to the shop theme and the product name `Cartin`; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: GitHub port `443` remains unreachable here, so no live remote issue/PR/commit refresh or `gh-pages` push was possible; the root workspace is still not a Git checkout; the recovery script preview server still does not persist after that PowerShell process exits, so post-closeout browser or HTTP verification remains unavailable from this shell.
- Next recommended action: push `.deploy-medivista-github` commit `c661911` from a network-enabled environment, then visually verify the public Home header/logo/mobile nav plus Products taxonomy parity after the source-data normalization.

## 2026-06-12 14:44:47 +09:00

- GitHub/local/deploy state checked: re-read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; confirmed the root workspace remains the local source of truth; `.deploy-medivista-github` now sits at `gh-pages...origin/gh-pages [ahead 16]`; live remote refresh remains blocked by GitHub port `443` failure in this environment.
- New issues or PRs found: no newer GitHub issue, PR, or remote commit information was accessible live in this run; the last known active items remain Issue `#11` and PR `#10`, and no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static subpages still share the same `v=20260611d` CSS/JS/logo references, `aria-controls="primary-nav"` and `id="primary-nav"` wiring remains present for mobile navigation, Brand Shop links still target `https://shop.medivista.co.kr`, English-first copy remains intact, required product categories are complete, and main-site catalog-only constraints remain intact; the one standards mismatch found in this run was the `BRANDS` submenu label, which showed `CELLEXOR` instead of the required `Cellexor Re:Tone`.
- Files changed: `index.html`, `about/index.html`, `products/index.html`, `brands/index.html`, `blogs/index.html`, `contact/index.html`, `cellexor/index.html`, `.deploy-medivista-github/index.html`, `.deploy-medivista-github/about/index.html`, `.deploy-medivista-github/products/index.html`, `.deploy-medivista-github/brands/index.html`, `.deploy-medivista-github/blogs/index.html`, `.deploy-medivista-github/contact/index.html`, `.deploy-medivista-github/cellexor/index.html`, `wp-theme-starter/header.php`, `docs/dev-log.md`, `docs/error-report-latest.md`, and `routine/issue-log.md`.
- Checks run: `git -c safe.directory=... status`, `log`, and deploy diff review for `.deploy-medivista-github`; root-vs-deploy SHA-256 parity checks for key static pages and shared CSS/JS; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; Brand Shop URL scan; runtime commerce/risky-claim scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: GitHub port `443` remains unreachable here, so no live remote issue/PR/commit refresh or `gh-pages` push was possible; the root workspace is still not a Git checkout; the recovery script preview server still does not persist after that PowerShell process exits, so post-closeout browser or HTTP verification remains unavailable from this shell.
- Next recommended action: push `.deploy-medivista-github` commit `979a55a` from a network-enabled environment, then visually verify the public Home header/logo/mobile nav and the updated `BRANDS` submenu label on desktop and mobile.

## 2026-06-12 17:43:48 +09:00

- GitHub/local/deploy state checked: re-read AGENTS.md, docs/dev-log.md, docs/github-client-preview.md, docs/medivista-web-automation-cycle.md, docs/medivista-automation-prompts.md, docs/error-resolution-automation.md, and docs/error-report-latest.md; GitHub connector confirmed the same open repo items remain Issue #11, Issue #3, and PR #10; the root workspace remains the local source of truth; .deploy-medivista-github is now gh-pages...origin/gh-pages [ahead 17] after this run's deploy commit, while direct git ls-remote to GitHub still fails on port 443 here.
- New issues or PRs found: no newer GitHub issues, PRs, or connector-visible committed repo changes beyond the existing open Issue #11, open Issue #3, and open PR #10; no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static subpages still share the same =20260611d CSS/JS/logo references, ria-controls="primary-nav" and id="primary-nav" mobile-nav wiring remain present, Brand Shop links still target https://shop.medivista.co.kr, English-first copy remains intact, catalog-only constraints still pass, and the one scoped compatibility fix in this run removed the stale legacy Cosmetics category from the Products page so the public catalog taxonomy now matches the current AGENTS/routine category list.
- Files changed: products/index.html, wp-theme-starter/page-products.php, ssets/data/medivista-products.csv, ssets/data/product-image-manifest.csv, ssets/data/product-category-classification.csv, wp-theme-starter/assets/data/medivista-products.csv, wp-theme-starter/assets/data/product-image-manifest.csv, wp-theme-starter/assets/data/product-category-classification.csv, docs/dev-log.md, docs/error-report-latest.md, outine/issue-log.md, and mirrored deploy checkout files committed as .deploy-medivista-github commit 2c7b2d9.
- Checks run: GitHub connector issue/PR refresh; deploy status, emote, and recent log; root-vs-deploy SHA-256 parity for Products page and mirrored data files; g scan confirming legacy Cosmetics taxonomy removal; 
ode --check assets/js/main.js; 
ode --check wp-theme-starter/assets/js/main.js; Brand Shop URL scan; runtime commerce/risky-claim scan with expected false positives limited to the product name Cartin; powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip.
- Blockers or network/auth limits: GitHub port 443 remains unreachable here, so no live remote fetch or gh-pages push was possible; the root workspace is still not a Git checkout; the recovery script preview server still does not persist after that PowerShell process exits, so post-closeout browser or HTTP verification remains unavailable from this shell.
- Next recommended action: push .deploy-medivista-github commit 2c7b2d9 from a network-enabled environment, then visually verify the public Home header/logo/mobile nav and the Products page category list after the legacy Cosmetics removal.

## 2026-06-15 20:21:29 +09:00

- GitHub/local/deploy state checked: re-read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; GitHub connector still shows open Issue `#11`, open Issue `#3`, and open PR `#10`; no newer connector-visible repo commit information overrode the local workspace as source of truth; direct `git ls-remote` to GitHub still failed on port `443`.
- New issues or PRs found: no new GitHub issues or PRs were exposed beyond Issue `#11`, Issue `#3`, and PR `#10`; no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static subpages still share `v=20260611d` CSS/JS/logo references, Brand Shop links still target `https://shop.medivista.co.kr`, English-first copy remains intact, required product categories remain complete, and main-site catalog-only constraints plus risky-claim checks still pass; mobile-nav structure remains wired through `aria-controls="primary-nav"` and `id="primary-nav"`, but browser-level verification stayed limited to the transient preview job and blocked public fetch.
- Files changed: `.deploy-medivista-github/wp-theme-starter/header.php`, `.deploy-medivista-github/.git` commit state (`d7549ac deploy: sync brand submenu label`), `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: GitHub connector issue/PR refresh; deploy `status`, `remote -v`, `log`, and `ls-remote` attempt; root-vs-deploy hash comparison for key static pages and shared CSS/JS; Brand Shop URL scan; category coverage scan; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; runtime commerce scan; runtime risky-claim scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: GitHub port `443` remains unreachable here, so no live remote fetch or `gh-pages` push was possible; the root workspace is still not a Git checkout; the recovery-script preview server still does not persist after that PowerShell process exits; the refreshed recovery report also shows pre-existing uncommitted deploy drift in `.deploy-medivista-github/assets/css/styles.css`, `.deploy-medivista-github/index.html`, `.deploy-medivista-github/wp-theme-starter/assets/css/main.css`, and `.deploy-medivista-github/wp-theme-starter/front-page.php`, which were preserved untouched in this run.
- Next recommended action: from a network-enabled environment, push `.deploy-medivista-github` commit `d7549ac`, then visually verify the public Home header/logo/mobile nav and the WordPress starter brand submenu label while also reviewing the older uncommitted deploy-only CSS/front-page drift before publishing.

## 2026-06-15 20:25:57 +09:00

- GitHub/local/deploy state checked: this was a local product asset sync request using the workspace product list zip and finished image folder; the root workspace remains the source of truth and is still not a Git checkout, while `.deploy-medivista-github` remained available for mirrored asset sync.
- New issues or PRs found: no live GitHub issue or PR refresh was performed for this scoped local upload; no duplicate branch or PR was created.
- Homepage compatibility result: product image upload did not change public copy, navigation, Brand Shop routing, or catalog-only behavior; the final safety scan passed, with `Hair Treatment` treated as the required category label rather than a risky medical claim.
- Files changed: `assets/images/products/*.webp`, `wp-theme-starter/assets/images/products/*.webp`, `.deploy-medivista-github/assets/images/products/*.webp`, `.deploy-medivista-github/wp-theme-starter/assets/images/products/*.webp`, `assets/data/product-image-manifest.csv`, `wp-theme-starter/assets/data/product-image-manifest.csv`, `products/index.html`, `index.html`, `wp-theme-starter/template-parts/product-card.php`, `wp-theme-starter/front-page.php`, `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: product list/image count check; Products page card and ready-image count check; root/deploy static and WordPress image count check; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; Brand Shop URL scan; commerce/risky-claim scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: deploy push was not executed here; public preview fetch remains blocked in this environment; the recovery-script preview server remains transient to that PowerShell process.
- Next recommended action: push `.deploy-medivista-github` from a network-enabled environment, then visually verify the Products page images and the two pending preparation placeholders for Body Fillers and Hair Treatment.

## 2026-06-15 20:30:38 +09:00

- GitHub/local/deploy state checked: scoped follow-up aligned Products upload sources to the Products dropdown taxonomy; root remains a non-git workspace and `.deploy-medivista-github` carried the mirrored deploy changes.
- New issues or PRs found: no live GitHub issue or PR refresh was performed for this scoped local alignment; no duplicate branch or PR was created.
- Homepage compatibility result: Products navigation remains wired to all 9 dropdown categories, and Products page blocks exist for every dropdown slug; no price, cart, checkout, or payment flow was added.
- Files changed: `assets/data/medivista-products.csv`, `wp-theme-starter/assets/data/medivista-products.csv`, `wp-theme-starter/template-parts/product-card.php`, mirrored deploy product CSVs, mirrored deploy WordPress starter product data, `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: old-name and legacy Cosmetics scan; dropdown category coverage check; product image manifest readiness check; Products page card/status count; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: deploy push was not executed here; public preview fetch remains blocked in this environment; the recovery-script preview server remains transient to that PowerShell process.
- Next recommended action: push `.deploy-medivista-github` from a network-enabled environment, then visually verify Products dropdown filtering and category anchors on the public site.

## 2026-06-15 21:12:04 +09:00

- GitHub/local/deploy state checked: this was a local attached-package refresh request; the root workspace remained the source of truth, and `.deploy-medivista-github` still shows pending local deploy drift plus deferred push state.
- New issues or PRs found: no live GitHub issue or PR refresh was performed for this package-only update; no duplicate branch or PR was created.
- Homepage compatibility result: no runtime copy, navigation, product taxonomy, Brand Shop routing, or catalog-only behavior was changed; the update only refreshed the WordPress starter ZIP artifact from the current local folder.
- Files changed: `wp-theme-starter.zip`, `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: ZIP content inspection before and after rebuild; `tar -tf wp-theme-starter.zip` validation; 114 WebP product-image count check; key WordPress starter file presence check; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; commerce/price/cart/checkout/payment scan; Brand Shop URL and risky-claim scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: public preview fetch remains blocked in this environment; deploy push was not executed; the recovery-script preview server remains transient to that PowerShell process.
- Next recommended action: use the refreshed `wp-theme-starter.zip` for the main WordPress theme upload, then visually verify Products images, partner logo slider, header navigation, and category anchors after applying it.

## 2026-06-16 02:17:18 +09:00

- GitHub/local/deploy state checked: re-read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; GitHub connector confirms repo `keithjeon-web/medivista` still uses default branch `medivista`, open Issue `#11`, open Issue `#3`, and open PR `#10`; connector-visible recent default-branch commits are still the older 2026-05-07 docs commits, so no newer remote information displaced the local workspace as source of truth.
- New issues or PRs found: no new GitHub issues or PRs beyond Issue `#11`, Issue `#3`, and PR `#10`; no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static pages still share `v=20260611d` CSS/JS/logo references, `aria-controls="primary-nav"` / `id="primary-nav"` mobile-nav wiring remains present, Brand Shop links still target `https://shop.medivista.co.kr`, English-first copy remains intact, required product categories remain complete, and catalog-only plus risky-claim constraints still pass; the one scoped compatibility issue found this run was an encoding-only root/deploy mismatch in `products/index.html`.
- Files changed: `products/index.html`, `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: GitHub connector repo / issue / PR / recent-commit review; deploy `status`, `remote -v`, and recent `log`; root-vs-deploy SHA-256 parity checks including `products/index.html`; shared nav/logo/cache-bust scan; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; category completeness scan; commerce / risky-claim / Brand Shop URL scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: direct `git ls-remote` to GitHub still fails on port `443`; the root workspace is still not a Git checkout; `.deploy-medivista-github` remains `gh-pages...origin/gh-pages [ahead 1]` with preserved uncommitted deploy drift in `assets/css/styles.css` and `wp-theme-starter/assets/css/main.css`; public preview fetch remains blocked here; the recovery-script preview server remains transient to that PowerShell process.
- Next recommended action: from a network-enabled environment, push `.deploy-medivista-github`, decide whether the deploy-only CSS spacing changes should be committed with the partner action slider work, then visually verify the public Home header/logo/mobile nav and Products slider spacing.

## 2026-06-19 11:07:00 +09:00

- GitHub/local/deploy state checked: re-read `AGENTS.md`, `docs/dev-log.md`, `docs/github-client-preview.md`, `docs/medivista-web-automation-cycle.md`, `docs/medivista-automation-prompts.md`, `docs/error-resolution-automation.md`, and `docs/error-report-latest.md`; confirmed the root workspace is still the source of truth and still not a Git checkout; verified the deploy checkout with `git -c safe.directory=... status`, `log`, `rev-parse`, `remote -v`, and `ls-remote`; GitHub app metadata confirms repo `keithjeon-web/medivista` still uses default branch `medivista`, remote `medivista` currently points to `8ce7701`, remote `gh-pages` to `a749a84`, local deploy `HEAD` is `5982567`, and open PR `#10` remains mergeable against an older base.
- New issues or PRs found: no new open PRs were exposed beyond PR `#10`; PR `#10` is still open and mergeable, with head `02049eb`; issue state could not be refreshed directly in this run because issue-list tooling was not available here, so no new issue was asserted; no duplicate branch or PR was created.
- Homepage compatibility result: homepage and key static subpages still share the same `v=20260611d` CSS/JS/logo references; `aria-controls="primary-nav"` and `id="primary-nav"` mobile navigation wiring remains present; Brand Shop links still target `https://shop.medivista.co.kr`; English-first copy remains intact; required product categories remain complete across static and WordPress starter surfaces; catalog-only and risky-claim constraints still pass; no new runtime compatibility defect required a content or code fix in this cycle.
- Files changed: `docs/error-report-latest.md`, `docs/dev-log.md`, and `routine/issue-log.md`.
- Checks run: deploy `status`, `remote -v`, `log`, `rev-parse`, `diff`, and `ls-remote`; root-vs-deploy SHA-256 parity checks for key static pages, shared CSS/JS, and WordPress starter runtime files; `node --check assets/js/main.js`; `node --check wp-theme-starter/assets/js/main.js`; `node --check wp-theme-shop/assets/js/shop.js`; Brand Shop URL scan; runtime commerce/risky-claim scan with expected hits confined to docs, tooling, README guidance, and the shop theme; product-category coverage scan; `powershell -ExecutionPolicy Bypass -File tools/medivista-error-recovery.ps1 -StartPreview -RebuildWordPressZip`.
- Blockers or network/auth limits: direct local HTTP checks to `http://127.0.0.1:4173/` still fail after the closeout because the recovery-script preview job does not persist after that PowerShell process exits; public preview fetch remains blocked here; deploy push was not attempted, but the recovery report still shows `.deploy-medivista-github` as `gh-pages...origin/gh-pages [ahead 1]` with preserved uncommitted CSS drift in `assets/css/styles.css` and `wp-theme-starter/assets/css/main.css`; `gh` is not available in PATH in this shell.
- Next recommended action: from a network-enabled environment, push `.deploy-medivista-github` commit `5982567`, decide whether the deploy-only CSS spacing changes should be committed before publish, then visually verify the public Home header/logo/mobile nav, partner action slider spacing, and Products page anchors after the `gh-pages` update.

## 2026-06-20 - PRODUCTS image-only catalog, Cosmetic taxonomy, and RUVENIS rename

- GitHub context checked: reviewed open Issue `#3` for PRODUCTS structure and Issue `#1` for product-list inquiry behavior before implementation.
- Result: PRODUCTS now visually shows category-grouped product photos only, with 114 ready-image cards across Botulinum Toxins, Dermal Fillers, Body Fillers, Skin Boosters, Lipolysis, Exosomes, Biostimulators, Hair Treatment, Vitamin Injections, and Cosmetic.
- Taxonomy: added `Vitamin Injections`; moved Guthion 1200mg, Jeil High B, and Vitamin C into it; renamed `Others` to `Cosmetic`.
- Brand: changed public `RUMINES` copy to `RUVENIS`; retained the `/rumines/` compatibility route.
- Mirrored surfaces: static site, `.deploy-medivista-github`, `wp-theme-starter`, product CSV sources, and WordPress page import XML.
- Checks run: JS syntax; XML parse; 114 card/image count; 10-category coverage; root/deploy SHA-256 parity; commerce/claim scan; recovery closeout with WordPress ZIP rebuild.
- Blocker: local preview server passed from PowerShell, but the in-app browser could not access that localhost process; public preview remains pending a network-enabled push.

## 2026-06-20 - CELLEXOR page and Brands/Contact 404 fix

- Finding: the WordPress theme templates existed, but missing WordPress page records could leave `/brands/`, `/cellexor/`, and `/contact/` returning 404 when the WXR import was not applied.
- Fix: added automatic creation and template assignment for the three core pages on theme activation, administrator load, and early initialization.
- CELLEXOR page: rebuilt the static and WordPress versions from a browser audit of the official reference, while excluding unsupported regulatory, patent, numerical, and efficacy claims.
- Links: all main CELLEXOR menu and CTA paths now use the dedicated `/cellexor/` route; official product and inquiry actions use the approved CELLEXOR URLs with `noopener noreferrer`.
- Documentation: added the page audit and design-token records.
- Validation: clean local routes returned HTTP 200; JS, fallback PHP lint, ZIP/XML, image, commerce, and claim checks passed; root/deploy parity passed.
- Remaining deployment action: upload/activate the rebuilt WordPress theme or deploy this branch so the live WordPress installation executes the page-provisioning hook.
