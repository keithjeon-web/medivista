const MEDIVISTA_WHATSAPP_NUMBER='821059066768';const MEDIVISTA_WHATSAPP_MESSAGE='Hello MEDIVISTA, I would like to inquire about your products.';function buildWhatsAppUrl(productName,customMessage){const message=customMessage||(productName?`${MEDIVISTA_WHATSAPP_MESSAGE} Product: ${productName}.`:MEDIVISTA_WHATSAPP_MESSAGE);return`https://wa.me/${MEDIVISTA_WHATSAPP_NUMBER}?text=${encodeURIComponent(message)}`}document.querySelectorAll('[data-whatsapp]').forEach((link)=>{link.setAttribute('href',buildWhatsAppUrl(link.dataset.product));link.setAttribute('target','_blank');link.setAttribute('rel','noopener noreferrer')});document.querySelectorAll('.site-header').forEach((header)=>{const toggle=header.querySelector('.nav-toggle');const nav=header.querySelector('.main-nav');if(!toggle||!nav)return;toggle.addEventListener('click',()=>{const isOpen=header.classList.toggle('nav-open');toggle.setAttribute('aria-expanded',String(isOpen))});nav.querySelectorAll('a').forEach((link)=>{link.addEventListener('click',()=>{header.classList.remove('nav-open');toggle.setAttribute('aria-expanded','false')})})});document.querySelectorAll('.product-image').forEach((visual)=>{const card=visual.closest('article');const title=card?.querySelector('h3')?.textContent?.trim();const meta=card?.querySelector('.card-meta')?.textContent?.trim();const label=title||meta||'MEDIVISTA Catalog';visual.dataset.label=label});document.querySelectorAll('.blog-thumb').forEach((visual)=>{const card=visual.closest('article');visual.dataset.label=card?.querySelector('h3')?.textContent?.trim()||'MEDIVISTA Insight'});document.querySelectorAll('.brand-visual').forEach((visual)=>{const section=visual.closest('section');visual.dataset.label=section?.querySelector('h1, h2')?.textContent?.trim()||'CELLEXOR'});document.querySelectorAll('[data-year]').forEach((node)=>{node.textContent=new Date().getFullYear()});
